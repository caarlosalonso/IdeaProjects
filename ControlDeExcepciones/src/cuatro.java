import java.util.Scanner;

public class cuatro {
    public static void main(String[] args) {
        Scanner teclado = new Scanner(System.in);
        // Genera un ejemplo similar al del Divisor, pero buscando el error “StringIndexOutOfBoundsException”
        //(acceso a una posición prohibida de un String).

    }
}
